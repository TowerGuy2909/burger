<h1>burger!</h1>

<h2>Burgers that are eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless devour}}
      {{> burger/devour-burger devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers devoured!</h2>

<ul>
  {{#each burger}}
    {{#if devour}}
      {{> burger/devour-burger sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" burger_name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devour?</label><br>
    <input type="radio" name="devour" value="1" checked> eaten!<br>
    <input type="radio" name="devour" value="0"> ready
  </div>

  <button type="submit">Add burger</button>
</form>
